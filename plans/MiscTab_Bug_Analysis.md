["Src/UI/Tabs/MiscTab.lua`](Src/UI/Tabs/MiscTab.lua:279-292).\n\n## Root Cause\n\n### Location\nFile: [`Src/UI/Tabs/MiscTab.lua`](Src/UI/Tabs/MiscTab.lua:279-292)\nLines: 279-292 (Anchor Items Toggle)\n\n### The Bug\n```lua\n-- Toggle 1: Anchor\nPhysicsSection:Toggle({\n    Flag = \"Physics.Anchor", "Title = \"\ud83d\udd12 Anchor Items", "Desc = \"Disable gravity & physics (items can't move)", "Value = false,\n    Callback = function(v)\n        if PhysicsOptimizer then\n            if v then\n                PhysicsOptimizer.SetAnchor(true)\n            else\n                PhysicsOptimizer.Revert(\"Anchored", "end,  -- \u274c MISSING 'end' to close the 'if v then' block\n    })\n```\n\n### Problem\nThe `Callback` function is missing an `end` statement to close the inner `if v then` block. This causes a Lua syntax error that prevents the entire [`MiscTab.lua`](Src/UI/Tabs/MiscTab.lua) file from being parsed and loaded.\n\n### Impact\n- The entire [`MiscTab.lua`](Src/UI/Tabs/MiscTab.lua) module fails to load\n- The Misc tab does not appear in the UI\n- All features in the Misc tab (Sound settings, Notifications, Fly, Speed, Lighting, Physics Optimizer) are unavailable\n\n## The Fix\n\n### Corrected Code\n```lua\n-- Toggle 1: Anchor\nPhysicsSection:Toggle({\n    Flag = \"Physics.Anchor", "Title = \"\ud83d\udd12 Anchor Items", "Desc = \"Disable gravity & physics (items can't move)", "Value = false,\n    Callback = function(v)\n        if PhysicsOptimizer then\n            if v then\n                PhysicsOptimizer.SetAnchor(true)\n            else\n                PhysicsOptimizer.Revert(\"Anchored", "end  -- \u2705 Added missing 'end'\n        end\n    end,\n})\n```\n\n### Changes Required\n1. Add `end` after line 290 (after `PhysicsOptimizer.Revert(\"Anchored", 2.0, "This closes the `if v then` block\n3. The outer `if PhysicsOptimizer then` block already has its `end` at line 291\n\n## Additional Analysis\n\n### PhysicsOptimizer.lua Status\nThe [`Src/Features/PhysicsOptimizer.lua`](Src/Features/PhysicsOptimizer.lua) file is **correctly structured** with no syntax errors. All required functions are properly defined:\n- `Init()`\n- `SetSelectedTypes()`\n- `ScanItemTypes()`\n- `SetAnchor()`\n- `Revert()`\n- `Apply()`\n- `YeetItems()`\n- `CreatePlatform()`\n- `MoveToStorage()`\n- `TeleportToStorage()`\n\n### MainInterface.lua Loading Process\nThe [`Src/UI/MainInterface.lua`](Src/UI/MainInterface.lua) correctly:\n1. Loads the PhysicsOptimizer module (line 123)\n2. Stores it in the Features bundle (line 172)\n3. Initializes it (line 243)\n4. Passes it to MiscTab.Create() (line 304)\n\nThe loading process is correct; the issue is purely the syntax error in MiscTab.lua.\n\n## Verification Steps\n\nAfter applying the fix:\n1. The MiscTab.lua module should load without errors\n2. The Misc tab should appear in the UI sidebar\n3. All features should be accessible:\n   - Mute All Game Sounds\n   - Disable Notifications\n   - Universal Fly (NoClip)\n   - Walk Speed (Persistent)\n   - Fullbright\n   - Remove Shadows\n   - Remove Fog\n   - Physics Optimizer section with all buttons\n\n## Related Files\n- [`Src/UI/Tabs/MiscTab.lua`](Src/UI/Tabs/MiscTab.lua) - Contains the bug\n- [`Src/Features/PhysicsOptimizer.lua`](Src/Features/PhysicsOptimizer.lua) - No issues found\n- [`Src/UI/MainInterface.lua`](Src/UI/MainInterface.lua) - No issues found"]